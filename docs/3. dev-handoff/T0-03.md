# DEV HANDOFF: T0-03

## 1. Thông tin chung
- Ticket: T0-03 — Khởi tạo repo + cấu trúc thư mục chuẩn
- Người thực hiện: Dev Agent
- Trạng thái: HOÀN THÀNH

## 2. Chi tiết công việc đã làm
- Khởi tạo local git repository, commit các thay đổi.
- Tạo cấu trúc thư mục theo đúng module map trong kiến trúc (tạo sẵn `__init__.py` cho các thư mục source code Python và `.gitkeep` cho các folder rỗng).
- Cập nhật `.gitignore` để bỏ qua các file không mong muốn như `*.dxf`, `output/`, `*.exe`, `.venv/`...
- Tạo `requirements.txt` với các dependency cơ bản: `ezdxf>=1.3`, `Shapely>=2.0`, `openpyxl>=3.1`, `PySide6>=6.6`, `pytest>=7.4`, `PyInstaller>=6.0`.
- Tạo `src/config/config.yaml` template với các section `cad`, `calculation`, `export`.
- Tạo `README.md` cơ bản chứa mô tả project và hướng dẫn cài đặt.

## 3. Các thay đổi (Files changed)
- `[MODIFY]` `.gitignore`
- `[NEW]` `README.md`
- `[NEW]` `requirements.txt`
- `[NEW]` `src/config/config.yaml`
- `[NEW]` `src/*` (intake, cad_parser, geometry, room_engine, door_engine, text_extractor, calc_engine, validation, export, ui, pipeline, config, utils)
- `[NEW]` `lisp/`, `tests/`, `samples/`, `packaging/`, `docs/`

## 4. Acceptance Criteria Checklist
- [x] AC-01: Repo tồn tại, team clone được (đã git init).
- [x] AC-02: Cấu trúc thư mục đúng theo module map trong kiến trúc (04-Kien-Truc-Tech-Stack.md §5).
- [x] AC-03: README.md có: mô tả project, requirements, bước clone và setup cơ bản.
- [x] AC-04: .gitignore bao gồm: `*.dxf`, `output/`, `dist/`, `*.exe`, `.venv/`, `__pycache__/`.
- [x] AC-05: requirements.txt có ezdxf, Shapely, openpyxl, PySide6, pytest, PyInstaller.
- [x] AC-06: config.yaml template có đủ các section: `cad`, `calculation`, `export`.

## 5. Notes / Blockers / Fallback
- Không có blocker. Repository đã có sẵn bộ khung (scaffold) để phục vụ cho các ticket khác trong Sprint 1.
