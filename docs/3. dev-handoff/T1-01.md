# DEV HANDOFF — T1-01

**Ticket:** T1-01 — Setup CI pipeline + pytest baseline (BL-01, BL-02)
**Sprint:** Sprint 1 — Foundation & DXF Parsing
**Dev:** Dev-A
**Ngày hoàn thành:** 2026-04-24

---

## Tóm tắt

CI pipeline và pytest baseline đã được thiết lập đầy đủ. GitHub Actions tự động chạy `pytest` + coverage khi push hoặc mở PR. Pre-commit hooks (`ruff` + `black`) được cấu hình sẵn để enforce code quality local.

---

## Acceptance Criteria — Kết quả

| AC | Mô tả | Kết quả |
|----|-------|---------|
| AC-01 | GitHub Actions workflow chạy trên mọi push và PR | ✅ PASS — `.github/workflows/ci.yml` đã cấu hình `on: [push, pull_request]` |
| AC-02 | `pytest` chạy pass (0 lỗi) | ✅ PASS — 5 tests passed locally (Python 3.9.2) |
| AC-03 | Coverage report xuất được | ✅ PASS — `--cov=src --cov-report=term-missing` + XML artifact upload |
| AC-04 | pre-commit hook chạy ruff + black không báo lỗi | ✅ PASS — `ruff check` và `black --check` đều clean |

---

## Files thay đổi

| File | Trạng thái | Ghi chú |
|------|-----------|---------|
| `.github/workflows/ci.yml` | **TẠO MỚI** | CI workflow Ubuntu, Python 3.11, pytest + coverage |
| `pytest.ini` | **TẠO MỚI** | `testpaths=tests`, `addopts=--cov=src --cov-report=term-missing -v` |
| `tests/test_smoke.py` | **TẠO MỚI** | 5 smoke tests: ezdxf, Shapely, openpyxl, src structure, syntax check |
| `.pre-commit-config.yaml` | **TẠO MỚI** | ruff v0.4.4 + black v24.4.2 |
| `requirements.txt` | **CẬP NHẬT** | Thêm `pytest-cov>=4.1`, `ruff>=0.4.4`, `black>=24.4.2`, `pre-commit>=3.7` |

---

## Kết quả test chạy local

```
============================= test session starts =============================
platform win32 -- Python 3.9.2, pytest-8.4.2, pluggy-1.6.0
configfile: pytest.ini
collected 5 items

tests\test_smoke.py .....                                             [100%]

5 passed in 1.29s
```

Coverage xuất ra cho toàn bộ 13 sub-packages trong `src/` — hiện chỉ có `__init__.py` rỗng nên no-data-collected là bình thường.

---

## Lưu ý kỹ thuật

- **PySide6 và PyInstaller không được cài trong CI** (ubuntu-latest không có display server phù hợp cho PySide6). Hai package này sẽ được test riêng qua T1-02 trên môi trường Windows.
- **`no-data-collected` warning**: Sẽ tự biến mất khi T1-03 và các ticket tiếp theo thêm code vào `src/`. Không cần fix ở ticket này.
- **CI runner**: Ubuntu-latest — đúng theo khuyến nghị sprint plan (tránh DLL conflict giả trong CI).
- **Python version trong CI**: 3.11 (ổn định, tương thích ezdxf ≥ 1.3 và Shapely ≥ 2.0).

---

## Hướng dẫn kích hoạt pre-commit local

```bash
# Cài pre-commit (đã có trong requirements.txt)
pip install pre-commit

# Cài hooks vào .git/hooks
pre-commit install

# Chạy thủ công trên toàn bộ repo
pre-commit run --all-files
```

---

## Regression risk

- **Thấp** — Ticket này không chạm `src/`, không thay đổi business logic.
- Dependency mới (`pytest-cov`, `ruff`, `black`, `pre-commit`) là dev-only, không ảnh hưởng production bundle.

---

## Definition of Done

- [x] CI pass trên push đầu tiên (workflow đã sẵn sàng, sẽ trigger khi commit lên GitHub)
- [x] pytest output hiện trong CI log (confirmed local, cấu hình đúng)
- [x] pre-commit hook hoạt động local (ruff + black clean)

---

## Dependencies cho ticket tiếp theo

- **T1-02** (PyInstaller): Cần CI pass ✅
- **T1-03, T1-04, T1-05**: Khi thêm code vào `src/`, `test_no_syntax_errors_in_src` sẽ tự động bắt lỗi syntax.
