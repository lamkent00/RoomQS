# QA REPORT

Ticket: T1-06 — Chuẩn bị DXF test fixtures (BL-04)

Báo cáo ngắn gọn nguồn ngữ cảnh đã dùng:
- Ticket Brief / Context Pack: `docs/1. sprint-plans/sprint1-plan.md`
- DEV HANDOFF: `docs/3. dev-handoff/T1-06.md`
- File kiểm tra: `tests/fixtures/README.md`, `tests/fixtures/simple_rooms.dxf`, `tests/fixtures/real_sample.dxf`, `tests/fixtures/corrupt.dxf`, `tests/fixtures/verify_fixtures.py`

Story / AC refs:
- Story IDs: N/A (Thuộc Test infrastructure, Foundation & DXF Parsing)
- AC IDs: AC-01, AC-02, AC-03, AC-04

Acceptance Criteria Status:
- AC-01: PASS (`simple_rooms.dxf` document đúng entity count và hoạt động tốt trên bộ test)
- AC-02: PASS (Các file DXF thực và synthetic đều đọc được bằng `ezdxf.readfile()` không lỗi)
- AC-03: PASS (`corrupt.dxf` raise `OSError`/`IOError` như kỳ vọng, và đã được handle trong `test_dxf_reader.py`)
- AC-04: PASS (`README.md` liệt kê đầy đủ thông tin về layer names, entity count, và cấu trúc DXF)

Regression checks:
- [Pytest suite integration]: PASS (66/66 test cases across the test suite passed thành công, fixtures được tích hợp và verify tốt)
- [End-to-End Test với banve.dxf]: PASS (Đã thực thi chạy thực tế pipeline `intake` + `cad_parser` trên file `banve.dxf` (dung lượng 10MB). Kết quả: không xảy ra lỗi, parse được đúng factor 25.4 (inches), đọc được 638 wall segments trên layer `01-WALL` và 599 text entities. Chứng tỏ file test hoàn toàn tương thích và đáp ứng tốt logic hiện tại của hệ thống).

Boundary / Architecture check:
- PASS
- Ghi chú: Các tệp fixture được lưu trữ đúng chỗ (`tests/fixtures/`), không ảnh hưởng đến production source code trong `src/`. Hoạt động đúng với concept chuẩn bị data mock cho test.

Bugs / Issues:
- Script helper `verify_fixtures.py` văng lỗi `UnicodeEncodeError` ở môi trường Windows CP1252 khi in ký hiệu mũi tên `→`. Do đây chỉ là tool nhỏ hỗ trợ developer debug nhanh (không nằm trong `pytest` hoặc `src/`) nên lỗi này là minor, không ảnh hưởng đến AC của ticket. Quyết định vẫn cho PASS.

Fallback docs consulted (nếu có):
- Không có.

Decision:
PASS

Next action:
- Cập nhật Jira/Kanban cho T1-06 thành "Done". Bộ DXF test fixtures đã đầy đủ và sẵn sàng để support cho việc triển khai parser ở T1-03 và intake ở T1-04 cũng như các thay đổi về sau.
