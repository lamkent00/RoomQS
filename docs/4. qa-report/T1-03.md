# QA REPORT

Ticket: T1-03 — CAD Parser — `RawCADData` (LINE/LWPOLYLINE + TEXT/MTEXT + apply unit_factor)

Báo cáo ngắn gọn nguồn ngữ cảnh đã dùng:
- Ticket Context Pack (Sprint 1 Plan - `docs/1. sprint-plans/sprint1-plan.md`)
- DEV HANDOFF (`docs/3. dev-handoff/T1-03.md`)
- Mã nguồn và kết quả Unit Test (`src/cad_parser/`, `tests/unit/test_dxf_reader.py`, `tests/fixtures/`)

Story / AC refs:
- Story IDs: N/A (Technical Component)
- AC IDs: AC-01, AC-02, AC-03, AC-04, AC-05, AC-06, AC-07, AC-08

Acceptance Criteria Status:
- AC-01: PASS (`parse_dxf` trả về `RawCADData` chứa đủ 3 trường hợp lệ)
- AC-02: PASS (Đọc thành công đúng số lượng 12 segment của layer wall từ fixture)
- AC-03: PASS (LWPOLYLINE được tách điểm thành các line segment chính xác, kể cả trường hợp `closed`)
- AC-04: PASS (Đọc đủ TEXT + MTEXT dưới dạng thô chưa qua xử lý strip)
- AC-05: PASS (Tọa độ được nhân với biến `unit_factor` trơn tru qua mọi test case 1.0, 25.4, 1000.0)
- AC-06: PASS (Chặn thành công các entity không thuộc `wall_layer_names` theo case-insensitive)
- AC-07: PASS (Raise các ngoại lệ OSError, DXFError tường minh nếu file DXF corrupt)
- AC-08: PASS (36/36 tests của dxf_reader passed thành công trong local testing giả lập CI)

Regression checks:
- [Test suite integration]: PASS (Chạy full suite pass 41/41, không phá vỡ 5 smoke tests ở T1-01)
- [Pre-commit checks]: FAIL_MINOR (Phát hiện lỗi format code khi gọi hook `black` trên các file của T1-02 là `src/main.py` và `src/ui/main_window.py`. Tuy nhiên các file scope T1-03 đều đạt chuẩn. Black đã tự động sửa file).

Boundary / Architecture check:
- PASS
- Ghi chú: `src/cad_parser/dxf_reader.py` thực hiện đọc file DXF nguyên chất, KHÔNG dính líu đến `shapely` hoặc `text_extractor`. Việc truyền tham số `unit_factor` thay vì tự đọc `$INSUNITS` chứng tỏ Dev đã đọc và tôn trọng ranh giới module.

Bugs / Issues:
- Không có bugs về mặt logic cho T1-03. Cần commit lại 2 file đã được tự động reformat bởi `black` (`src/main.py` và `src/ui/main_window.py`) để các PR tiếp theo qua được pre-commit CI.

Fallback docs consulted (nếu có):
- Không có.

Decision:
PASS

Next action:
- Hoàn thành nghiệm thu T1-03.
- Đội dev cập nhật và commit code format từ Black.
- Dự án sẵn sàng thực thi T1-04 (Intake - Detect unit) và T1-05 (Text Extractor strip MTEXT).
