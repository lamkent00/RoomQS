# QA REPORT

Ticket: T1-04 — File Intake — detect `$INSUNITS` → `unit_factor`

Báo cáo ngắn gọn nguồn ngữ cảnh đã dùng:
- Ticket Brief / Sprint Plan: `docs/1. sprint-plans/sprint1-plan.md`
- DEV HANDOFF: `docs/3. dev-handoff/T1-04.md`
- File kiểm tra: `src/intake/unit_detector.py`, `src/intake/models.py`, `tests/unit/test_unit_detector.py`

Story / AC refs:
- AC IDs: AC-01, AC-02, AC-03, AC-04, AC-05

Acceptance Criteria Status:
- AC-01: PASS (`$INSUNITS=4` (mm) → factor = 1.0)
- AC-02: PASS (`$INSUNITS=6` (metres) → factor = 1000.0)
- AC-03: PASS (`$INSUNITS=1` (inches) → factor = 25.4)
- AC-04: PASS (`$INSUNITS=0` → factor = 1.0 + log warning "Don vi ve khong xac dinh")
- AC-05: PASS (Unit test pass với mock header không cần file DXF thực, test pass 100%)

Regression checks:
- Module `intake` là module độc lập mới tạo. Lệnh pytest pass toàn bộ test case (9/9) không ảnh hưởng tới module hiện có. PASS.

Boundary / Architecture check:
- PASS.
- Ghi chú: Module tuân thủ tuyệt đối giới hạn kỹ thuật (chỉ đọc header bằng ezdxf, không parse entities). Tương tác qua dataclass `IntakeResult` (immutable, frozen=True). Không sử dụng Shapely hay UI.

Bugs / Issues:
- Không có.

Fallback docs consulted (nếu có):
- Không có (Ticket Brief trong Sprint Plan và Dev Handoff đã đầy đủ thông tin về mapping ADR-03).

Decision:
PASS

Next action:
- Cập nhật trạng thái "Done" cho T1-04. Các module tiếp theo (đặc biệt T1-03) có thể tự tin sử dụng `IntakeResult` và `detect_unit_factor` cho pipeline.
