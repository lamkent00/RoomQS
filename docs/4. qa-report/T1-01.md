# QA REPORT

Ticket: T1-01 — Setup CI pipeline + pytest baseline

Báo cáo ngắn gọn nguồn ngữ cảnh đã dùng:
- Ticket Context Pack (Sprint 1 Plan - `docs/1. sprint-plans/sprint1-plan.md`)
- DEV HANDOFF (`docs/3. dev-handoff/T1-01.md`)
- File config CI/CD & test (`.github/workflows/ci.yml`, `pytest.ini`, `tests/test_smoke.py`, `.pre-commit-config.yaml`)

Story / AC refs:
- Story IDs: N/A (Technical Task)
- AC IDs: AC-01, AC-02, AC-03, AC-04

Acceptance Criteria Status:
- AC-01: PASS (GitHub Actions workflow được setup đúng với `on: [push, pull_request]`)
- AC-02: PASS (Lệnh `pytest` đã chạy thành công 5 smoke tests)
- AC-03: PASS (Coverage report xuất thành công qua `pytest --cov=src`)
- AC-04: PASS (Pre-commit hook chạy `ruff` + `black` hoàn thành không báo lỗi)

Regression checks:
- [CI/CD Config]: PASS (Chỉ thêm cấu hình mới cho workflow và pre-commit, không làm gián đoạn code base hiện tại)
- [Testing Base]: PASS (Thiết lập thành công nền tảng testing an toàn cho các sprint tiếp theo)

Boundary / Architecture check:
- PASS
- Ghi chú: Ticket tuân thủ tuyệt đối phạm vi boundary; chỉ chỉnh sửa file cấu hình và thư mục `tests/`. Không xâm phạm vào `src/` module. Tech stack (Ubuntu, Python 3.11, Pytest, Ruff, Black) phù hợp với quy định của dự án.

Bugs / Issues:
- Không phát hiện lỗi. Note: cảnh báo `no-data-collected` cho coverage trong `src/` là đúng kỳ vọng vì mã nguồn chính chưa được đưa vào.

Fallback docs consulted (nếu có):
- Không có.

Decision:
PASS

Next action:
- Ticket đã nghiệm thu hoàn thành. Đội dev có thể tiếp tục triển khai các ticket liên quan đến viết logic (T1-02, T1-03, ...) dựa trên baseline CI/CD này.
