# QA REPORT

Ticket: T1-05 — Text Extractor (MOD-06) — strip MTEXT, normalize text entities

Báo cáo ngắn gọn nguồn ngữ cảnh đã dùng:
- Ticket Brief / Context Pack: `docs/1. sprint-plans/sprint1-plan.md`
- DEV HANDOFF: `docs/3. dev-handoff/T1-05.md`
- File kiểm tra: `src/text_extractor/processor.py`, `src/text_extractor/models.py`, `tests/unit/test_text_processor.py`

Story / AC refs:
- Story IDs: N/A (Thuộc Foundation & DXF Parsing)
- AC IDs: AC-01, AC-02, AC-03, AC-04, AC-05

Acceptance Criteria Status:
- AC-01: PASS (TEXT entity raw `"PHONG NGU"` → `TextTag.content = "PHONG NGU"`)
- AC-02: PASS (MTEXT raw `"{\fArial;PHONG NGU}"` → `TextTag.content = "PHONG NGU"`)
- AC-03: PASS (MTEXT `"\PPHONG NGU"` → `TextTag.content = "PHONG NGU"`)
- AC-04: PASS (`position` và `layer` được pass through không thay đổi)
- AC-05: PASS (Unit test pass, không cần file DXF — dùng mock `RawTextEntity` list)

Regression checks:
- [Pytest Unit tests]: PASS (16/16 test cases passed, coverage 100% cho `src/text_extractor/`)

Boundary / Architecture check:
- PASS
- Ghi chú: Logic xử lý text hoàn toàn độc lập, sử dụng built-in `re` thay vì đụng đến `ezdxf` API. Data type pass qua lại tuân thủ tuyệt đối giới hạn (input `RawTextEntity`, output `TextTag`). Position / layer giữ nguyên float. Không tham chiếu đến UI hay thuật toán Shapely nào.

Bugs / Issues:
- Không có.

Fallback docs consulted (nếu có):
- Không có.

Decision:
PASS

Next action:
- Cập nhật trạng thái "Done" cho T1-05 trên hệ thống tracking.
- Chuyển đầu ra danh sách `TextTag` này sang thành phần đầu vào cho xử lý hình học (Spatial lookup) ở Sprint 3.
