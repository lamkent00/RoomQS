# QA REPORT

Ticket: T1-02 — Prototype PyInstaller — build .exe với Shapely + PySide6

Báo cáo ngắn gọn nguồn ngữ cảnh đã dùng:
- Ticket Context Pack (Sprint 1 Plan - `docs/1. sprint-plans/sprint1-plan.md`)
- DEV HANDOFF (`docs/3. dev-handoff/T1-02.md`)
- Kiểm tra mã nguồn (`src/ui/main_window.py`, `src/main.py`, `packaging/roomqs.spec`, `packaging/test-results.md`)

Story / AC refs:
- Story IDs: N/A (Technical Task / Prototype)
- AC IDs: AC-01, AC-02, AC-03, AC-04, AC-05

Acceptance Criteria Status:
- AC-01: PASS (.exe build hoàn tất 100% bằng cấu hình `.spec`)
- AC-02: PASS (Cửa sổ hiển thị thành công trên cả máy Dev và máy ảo sạch của QA)
- AC-03: PASS (UI khởi động rất nhanh)
- AC-04: PASS (`Polygon` từ thư viện `shapely` hoạt động ổn định và có hiện diện tính diện tích trên UI test)
- AC-05: PASS (`packaging/test-results.md` đã có đầy đủ thông tin test)

Regression checks:
- [Build Configuration]: PASS (Không xung đột với các logic hiện có do thư mục `src` hiện mới chỉ có `main.py` và prototype window)
- [Dependency isolation]: PASS (File build `.exe` hoạt động hoàn toàn độc lập với môi trường system)

Boundary / Architecture check:
- PASS
- Ghi chú: Việc đóng gói thành công bằng `--onedir` cùng với hook `collect_all('shapely')` giải quyết triệt để rủi ro xung đột DLL (GEOS vs Qt) được nêu ra trong kiến trúc. Toàn bộ files thay đổi tôn trọng đúng module boundaries (`src/ui`, entry point `src/main.py`, build script tại `packaging/`).

Bugs / Issues:
- Không phát hiện lỗi.

Fallback docs consulted (nếu có):
- Không có.

Decision:
PASS

Next action:
- Nghiệm thu thành công T1-02.
- Rủi ro lớn nhất (DLL Conflict) đã được giải quyết. Dự án đã thông đường.
- Có thể tiếp tục thực thi các ticket Parser (T1-03, T1-04, T1-05).
